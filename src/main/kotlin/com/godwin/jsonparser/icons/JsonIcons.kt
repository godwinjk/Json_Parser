package com.godwin.jsonparser.icons

import com.intellij.openapi.util.IconLoader

interface JsonIcons {
    object JsonIcons {
        @JvmField
        val jsonLogo = IconLoader.getIcon("/icons/json_logo.png", javaClass)

    }
}